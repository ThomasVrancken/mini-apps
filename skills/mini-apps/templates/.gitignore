# Secrets
.env
app.yaml

# Dependencies and build output
node_modules/
client/node_modules/
client/dist/
.venv/

# Local data dumps (backups before migrations can contain personal data)
backups/

.DS_Store
*.log
